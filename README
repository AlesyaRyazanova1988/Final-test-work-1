Задача: Написать программу, которая из имеющегося массива строк формирует новый массив из строк, длина которых меньше, либо равна 3 символам. 
Первоначальный массив можно ввести с клавиатуры, либо задать на старте выполнения алгоритма. 
При решении не рекомендуется пользоваться коллекциями, лучше обойтись исключительно массивами.

Описание решения:
1. Создаем массив строк с заданными значениями массива
2. Для наглядности выводим массив на экран (это не обязательно, но мне так захотелось) с помощью метода PrintArray*
3. Создаем второй массив строк без указанных значений, но с указанной длиной массива равной длине первого массива строк
4. Вводим числовую переменную (с типом int) j=0 для обозначения индекса элемента второго массива, с которого мы начнем его заполнение
5. Далее первый массив оборачиваем в цикл for c проверкой условия (if), в котором последовательно каждое значение первого массива
   будет проверяться на соблюдение условия, что если длина значения текущего индекса первого массива меньше или равна трем, то программа
   должна записать это значение в соответвующий элемент текущего индекса второго массива.
6. После того, как прогрмма закончит проверять все элементы первого массива, она должна вывести на экран второй массив с помощью метода PrintArray*

*Метод PrintArray описываем отдельно - метод типа void (не возвращает значение), принимает на вход массив строк
Далее оборачивает массив в цикл for, который так же перебирает каждый элемент массива и выводит на экран текущее значение с кавычками и пробелом.
В итоге, когда массив выходит из цикла на экран выводятся все элементы, которые есть в массиве в кавычках через пробел.
